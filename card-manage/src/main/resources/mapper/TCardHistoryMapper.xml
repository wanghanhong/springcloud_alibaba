<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.card.manage.mapper.TCardHistoryMapper">


    <select id="tCardHistoryList" parameterType="com.smart.card.manage.entity.TCardHistory" resultType="com.smart.card.manage.entity.TCardHistory">
        SELECT * from t_card_history t where t.delete_flag = 0
        <if test="vo.orderNumber != null ">
            and t.order_number like concat('%',#{vo.orderNumber},'%')
        </if>
        <if test="vo.status != null ">
            and t.status = #{vo.status}
        </if>
        <if test="vo.type != null ">
            and t.type = #{vo.type}
        </if>
        <if test="vo.orderType != null ">
            and t.order_type = #{vo.orderType}
        </if>
        <if test="vo.startTime != null and vo.startTime != '' ">
            and t.create_time &gt;= #{vo.startTime}
        </if>
        <if test="vo.endTime != null and vo.endTime != '' ">
            and t.create_time &lt;= #{vo.endTime}
        </if>
        <if test="vo.deptIds != null and vo.deptIds != '' ">
            and t.dept_id in (${vo.deptIds})
        </if>
        ORDER BY t.create_time desc
    </select>


</mapper>
