<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.card.manage.mapper.TCardUseMapper">


    <select id="tCardUseList" parameterType="com.smart.card.manage.entity.TCardUse" resultType="com.smart.card.manage.entity.TCardUse">
        SELECT * from t_card_use t where t.delete_flag = 0
        <if test="vo.msisdn != null ">
            and t.msisdn like concat('%',#{vo.msisdn},'%')
        </if>
        <if test="vo.resourceName != null  ">
            and t.resource_name = #{vo.resourceName}
        </if>
        <if test="vo.startDate != null and vo.startDate != '' ">
            and t.start_time &gt;= #{vo.startDate}
        </if>
        <if test="vo.endDate != null and vo.endDate != '' ">
            and t.start_time &lt;= #{vo.endDate}
        </if>
        <if test="vo.deptIds != null and vo.deptIds != '' ">
            and t.dept_id in (${vo.deptIds})
        </if>
    </select>

    <select id="getUse" resultType="com.smart.card.manage.entity.TCardUse">
        SELECT * from t_card_use t where t.delete_flag = 0
        and t.msisdn = #{accNbr}
    </select>


</mapper>
