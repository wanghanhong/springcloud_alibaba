<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.card.manage.mapper.TBaseDictTypeMapper">


    <select id="tBaseDictTypeList" parameterType="com.smart.card.manage.entity.TBaseDictType" resultType="com.smart.card.manage.entity.TBaseDictType">
        SELECT * from t_base_dict_type t where t.delete_flag = 0
        <if test="vo.deptIds != null and vo.deptIds != '' ">
            and t.dept_id in (${vo.deptIds})
        </if>
        ORDER BY t.create_time desc
    </select>


</mapper>
