<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.device.install.mapper.TManagerCamerasMapper">

    <select id="managerCamerasList" parameterType="com.smart.device.common.install.entity.TManagerCameras" resultType="com.smart.device.common.install.entity.TManagerCameras">
        SELECT * from t_manager_cameras t where t.delete_flag = 0
        <if test="vo.sn != null ">
            and t.sn = #{vo.sn}
        </if>
        <if test="vo.buildingId != null  ">
            and t.building_id = #{vo.buildingId}
        </if>
        <if test="vo.companyId != null  ">
            and t.company_id = #{vo.companyId}
        </if>
        <if test="vo.companyName != null and vo.companyName != ''">
            and t.company_name like concat('%',#{vo.companyName},'%')
        </if>
        <if test="vo.opUserId != null ">
            and t.op_user_id = #{vo.opUserId}
        </if>
        <if test="vo.deviceState != null  ">
            and t.device_state = #{vo.deviceState}
        </if>
        <if test="vo.province != null and vo.province != ''">
            and t.province = #{vo.province}
        </if>
        <if test="vo.city != null and vo.city != ''">
            and t.city = #{vo.city}
        </if>
        <if test="vo.county != null and vo.county != ''">
            and t.county = #{vo.county}
        </if>
        <if test="vo.town != null and vo.town != ''">
            and t.town = #{vo.town}
        </if>
        order by t.create_time desc
    </select>
    <select id="getManagerCameras" parameterType="com.smart.device.common.install.entity.TManagerCameras" resultType="com.smart.device.common.install.entity.TManagerCameras">
        SELECT * from t_manager_cameras t where t.delete_flag = 0
        and t.sn = #{vo.sn} limit 1
    </select>


</mapper>
