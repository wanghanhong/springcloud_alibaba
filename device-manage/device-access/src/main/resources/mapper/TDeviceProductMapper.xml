<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.device.access.mapper.TDeviceProductMapper">

    <select id="queryProductList" resultType="com.smart.device.common.access.entity.TDeviceProduct" >
        select * from t_device_product where delete_flag = 0
    </select>





    <select id="getProductType" resultType="java.lang.String">
        select product_type  productType from t_device_product
        where product_name = #{productName} or product_code = #{productName}
    </select>

    <select id="getProductId" resultType="java.lang.Long">
        select id
        from t_device_product
        where product_name = #{productName} or product_code = #{productName}
    </select>




</mapper>
