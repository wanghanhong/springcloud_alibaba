<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.device.message.data.mapper.TBaseMsgMapper">


    <select id="dictList" resultType="com.smart.device.common.message.entity.TMsgDict" parameterType="java.lang.String">
        select dict_id dictId,dict_name dictName,status  from t_msg_dict t
        where t.delete_flag = 0
        <if test="dictTypeId != null ">
            and t.dict_type_id = #{dictTypeId}
        </if>
        ORDER BY id
    </select>

</mapper>
