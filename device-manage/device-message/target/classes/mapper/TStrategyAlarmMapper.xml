<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.device.message.data.mapper.TStrategyAlarmMapper">

    <select id="queryStrategys" parameterType="com.smart.device.common.message.entity.TStrategyAlarm" resultType="com.smart.device.common.message.entity.TStrategyAlarm">
        select * from t_strategy_alarm_type t where t.delete_flag = 0
        <if test="vo.parentType != null ">
            and t.parent_type = #{vo.parentType}
        </if>
        order by t.create_time desc
    </select>
    <select id="queryStrategyByTypeAndCode" parameterType="com.smart.device.common.message.entity.TStrategyAlarm" resultType="com.smart.device.common.message.entity.TStrategyAlarm">
        select * from t_strategy_alarm t where t.delete_flag = 0
            and t.device_type = #{vo.deviceType}
            and t.event_code = #{vo.eventCode}
        order by t.create_time desc
    </select>

</mapper>
