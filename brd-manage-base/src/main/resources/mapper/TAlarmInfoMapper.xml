<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.brd.manage.base.mapper.TAlarmInfoMapper">


    <select id="tAlarmInfoList" parameterType="com.smart.brd.manage.base.entity.TAlarmInfo" resultType="com.smart.brd.manage.base.entity.TAlarmInfo">
        SELECT * from t_alarm_info t where t.delete_flag = 0
        <if test="vo.eventStatus != null and vo.eventStatus != '' ">
            and t.event_status = #{vo.eventStatus}
        </if>
        <if test="vo.alarmCategory != null and vo.alarmCategory != '' ">
            and t.alarm_category = #{vo.alarmCategory}
        </if>
        <if test="vo.equipmentType != null and vo.equipmentType != '' ">
            and t.equipment_type = #{vo.equipmentType}
        </if>
        <if test="vo.shedId != null ">
            and t.shed_id = #{vo.shedId}
        </if>
        <if test="vo.bedId != null ">
            and t.bed_id = #{vo.bedId}
        </if>
        <if test="vo.startTime != null and vo.startTime != '' ">
            and DATE(t.alarm_time) &gt;= #{vo.startTime}
        </if>
        <if test="vo.endTime != null and vo.endTime != '' ">
            and DATE(t.alarm_time) &lt;= #{vo.endTime}
        </if>
        <if test="vo.deptIds != null and vo.deptIds != '' ">
            and t.dept_id in (${vo.deptIds})
        </if>
        ORDER BY t.event_status, t.create_time desc
    </select>


</mapper>
