<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.brd.manage.base.mapper.TVaccineRecordMapper">


    <select id="vaccineRecordList" parameterType="com.smart.brd.manage.base.entity.TVaccineRecord" resultType="com.smart.brd.manage.base.entity.TVaccineRecord">
        SELECT * from t_vaccine_record t where t.delete_flag = 0
        <if test="vo.preventionId != null and vo.preventionId != '' ">
            and t.prevention_id = #{vo.preventionId}
        </if>
        <if test="vo.deptIds != null and vo.deptIds != '' ">
            and t.dept_id in (${vo.deptIds})
        </if>
        ORDER BY t.create_time desc
    </select>


</mapper>
